import{describe,expect,it,vi}from"vitest";import{CodingProvider,type CodingRequest}from"../src/coding-provider.js";
const base: CodingRequest={provider:"codex",objective:"Correggi il progetto ed esegui i test",workspace:"C:/work/project",allowedRoot:"C:/work",maxCostCents:200};
describe("coding provider",()=>{it("binds approval to the exact action",async()=>{const runner=vi.fn(async()=>({stdout:"ok",stderr:"",exitCode:0})),provider=new CodingProvider(runner),estimate=provider.estimate(base);await expect(provider.execute({...base,approvedFingerprint:"wrong"})).rejects.toThrow(/not_approved/);await expect(provider.execute({...base,approvedFingerprint:estimate.fingerprint})).resolves.toMatchObject({output:"ok",exitCode:0});expect(runner).toHaveBeenCalledWith("codex",expect.arrayContaining(["exec","--json"]),expect.any(String),expect.any(Number))});it("rejects workspaces outside the configured root",()=>{expect(()=>new CodingProvider().estimate({...base,workspace:"C:/other"})).toThrow(/outside/)})});
