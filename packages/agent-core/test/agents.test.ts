import{describe,expect,it}from"vitest";import{agents,ToolRegistry}from"../src/index.js";describe("agents",()=>{it("defines bounded specialists",()=>{for(const a of Object.values(agents)){expect(a.timeoutMs).toBeGreaterThan(0);expect(a.maxCostCents).toBeGreaterThanOrEqual(0);expect(a.qualityCriteria.length).toBeGreaterThan(0)}});it("does not permit duplicate tools",()=>{const r=new ToolRegistry();const t={name:"read",schema:{},permissions:["read"],execute:async()=>true};r.register(t);expect(()=>r.register(t)).toThrow(/Duplicate/)})});
