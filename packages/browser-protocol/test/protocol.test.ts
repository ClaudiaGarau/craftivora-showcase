import{describe,expect,it}from"vitest";import{PROTOCOL_VERSION,requiresApproval,signRequest,verifyRequest}from"../src/index.js";const base={version:PROTOCOL_VERSION,requestId:"r1",origin:"https://example.test",action:"snapshot" as const,params:{},risk:"read" as const,preview:"Read current page",expiresAt:new Date(Date.now()+60000).toISOString(),sessionId:"s1"};describe("browser protocol",()=>{it("accepts an authentic unexpired request",()=>expect(verifyRequest(signRequest(base,"a secure session secret"),"a secure session secret")).toBe(true));it("rejects tampering",()=>{const r=signRequest(base,"a secure session secret");r.origin="https://evil.test";expect(verifyRequest(r,"a secure session secret")).toBe(false)});it("gates browser mutations",()=>{expect(requiresApproval("fill")).toBe(true);expect(requiresApproval("snapshot")).toBe(false)})});
